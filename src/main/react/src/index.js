import React from 'react';
import ReactDOM from 'react-dom';
import PreferredNames from './PreferredNames';
import './index.css';

ReactDOM.render(
  <PreferredNames />,
  document.getElementById('root')
);
